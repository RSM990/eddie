{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug eddie (module)",
      "type": "python",
      "request": "launch",
      "module": "etl.main",
      "args": ["--season", "2025", "--week", "1"],
      "cwd": "${workspaceFolder}",
      "envFile": "${workspaceFolder}/.env",
      "env": {
        "PYTHONPATH": "${workspaceFolder}/src"
      },
      "console": "integratedTerminal"
    }
  ]
}
